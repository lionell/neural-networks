#include <cstdio>
#include <cstdlib>
#include <list>
#include "NeuralNetwork.h"

int main()
{
	//printf("Hello world!\n");
	//Neuron* A = new Neuron();
	//A->SetCore(Sigmoid);
	//Neuron** Input = new Neuron*[8];
	//for (int i = 0; i < 8; i++)
	//{
	//	Input[i] = new Neuron();
	//	Input[i]->Attach(A, 0.0);
	//}
	//(Input[7]->GetSynapse(A))->SetWeight(1.0);
	//for (int i = 0; i < 8; i++) Input[i]->GenerateImpulse(5.0);
	//A->Activate();
	//printf("%f", A->GetImpulse());
	std::list<int> a;
	system("pause");
}